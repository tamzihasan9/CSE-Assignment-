#include<bits/stdc++.h>
using namespace std;

main(){
    int a,b;

    cout<<"Enter two numbers :"<<endl;

    cin>>a>>b;

    //++a means a is increment by 1;

    cout<< "a ++ :" << ++a<<endl;

    //--b means b is decrease by 1


    cout<< "b--  :" << --b<<endl;

    // a+=b means a=a+b;

    cout<< "a+=b : "<<(a+=b)<<endl;
    //a+=b means a=a-b;
     cout<< "a-=b : "<<(a-=b)<<endl;


      //a*=b means a=a*b;
     cout<< "a*=b : "<<(a*=b)<<endl;

 //a/=b means a=a/b;
     cout<< "a/=b : "<<(a/=b)<<endl;

 //a%+b means a=a%b;
     cout<< "a%=b : "<<(a%=b)<<endl;






return 0;
}
